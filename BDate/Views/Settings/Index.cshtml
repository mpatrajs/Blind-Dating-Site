@model IEnumerable<BDate.Models.Setting>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.isHiddenAge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isHiddenLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Profile)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.isHiddenAge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isHiddenLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Profile.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SettingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SettingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SettingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
