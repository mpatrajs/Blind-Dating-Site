@model IEnumerable<BDate.Models.Personality>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PersonalityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PersonalityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PersonalityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PersonalityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PersonalityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
